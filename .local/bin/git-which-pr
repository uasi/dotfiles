#!/usr/bin/env perl

use v5.26;
use warnings;

my @output = `git-when-merged -l $ARGV[0]`;

for my $line (@output) {
    $line =~ /Merge pull request #(\d+)/;
    if ($1) {
        exec qw(gh pr view -w), $1;
    }
}
